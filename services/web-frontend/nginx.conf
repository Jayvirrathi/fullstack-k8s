server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # --- Security headers (safe defaults for static sites) ---
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
  # If you terminate TLS in Nginx (443), add HSTS there (NOT on plain :80):
  # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  # --- Compression (ok to keep global as well) ---
  gzip on;
  gzip_types text/plain text/css application/javascript application/json application/xml image/svg+xml;
  gzip_min_length 1024;
  gzip_vary on;
  gzip_comp_level 6;

  # Health endpoint (no logs, no cache)
  location = /healthz {
    default_type text/plain;
    add_header Cache-Control "no-cache";
    access_log off;
    return 200 "ok\n";
  }

  # Vite builds hashed assets under /assets; cache them long
  location ^~ /assets/ {
    access_log off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }

  # Other static assets (fallback pattern)
  location ~* \.(?:js|css|woff2?|ttf|eot|otf|svg|png|jpg|jpeg|gif|webp|ico)$ {
    access_log off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }

  # Don’t cache the HTML entrypoint
  location = /index.html {
    add_header Cache-Control "no-cache";
    try_files $uri =404;
  }

  # SPA fallback for client-side routing
  location / {
    try_files $uri $uri/ /index.html;
  }
}
